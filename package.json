{
  "name": "samplecodes",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "ui-run": "cypress run --spec cypress/e2e/**.js --browser firefox --env allure=true",
    "allure-report": "allure generate allure-results --clean -o allure-report"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^12.3.0",
    "force": "^0.0.3"
  },
  "devDependencies": {
    "@shelex/cypress-allure-plugin": "^2.34.0"
  }
}
